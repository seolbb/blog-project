spring:
  application:
    name: blog_project

  datasource:
    url: jdbc:mysql://localhost:3306/blog
    username: seolb
    password: seolb1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 80

